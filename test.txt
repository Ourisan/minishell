TO TEST:

ALL:
    Make sure all error messages are correct (command not found, ...)
    Check for leaks
    Store status of last command in a var
    Check if var expansion works

MAIN:
    test Ctrl+C and Ctrl+D

PARSER:
    manage \"

EXECVE:
    test for file access after testing if file exists

REDIR :
    heredoc combine with pipe, (separer open() et dup2())
    remove EOF at the end of heredoc
    multiple heredoc

TEST hS :
    echo << EOF | wc -l > count.txt